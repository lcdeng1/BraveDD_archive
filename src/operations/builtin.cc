#include "builtin.h"

namespace BRAVE_DD {
    // implementation TBD
    Func saturate(Func set, FuncArray forwards)
    {
        // if (set.getForest()->getSetting().isRelation()) {
        //     throw error(ErrCode::INVALID_ARGUMENT, __FILE__, __LINE__);
        // }
        // if (!forwards.getForest()->getSetting().isRelation()) {
        //     throw error(ErrCode::INVALID_ARGUMENT, __FILE__, __LINE__);
        // }
        Func result;
        // TBD
        return result;
    }
}